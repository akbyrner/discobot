# discobot
Testing implementations of a discord bot

# slash-commands
https://discordjs.guide/legacy/app-creation/creating-commands
Once you have set up a new slash command, run `node deploy-commands.js` from root to add the command to the bot

# local versions
local versions for testing can be run with `node index.js`. Long term versions must be pulled to the local directory and run with PM2.